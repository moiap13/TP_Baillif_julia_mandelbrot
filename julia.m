% Auteur :  Antonio Pisanello% TP2 - Julia et Mandelbrot% Version : 1.0clear all%c = 0.214+0.62i;%c = 0.4+0.62i;%c = 0.285+0.01ic = -0.414 - 0.612i %Nombre complexe de référenceq = (1 + sqrt(4*abs(c)))/2; %limitearray_size = 500;iteration = 500; x = linspace(-q,q,array_size);y = x;[a,b] = meshgrid(x,y);w = a + i*b;m = zeros(array_size,array_size);for t = 1:iteration	w = ((w.^2)+c); %on met la valeur de W au carré et on			%l'incrément de c  	m = m + (abs(w) < q); 	%on stoque dans m si le module de w				%est plus petit que la limiteendforcolormap([ocean() ; flipud(jet())])imagesc(x,y,abs(m)) 	%on affiche les couleurs en fonction du nombre 			%de fois ou la condition fût truetitle('c = -0.414 - 0.612i');print("julia_4.png");